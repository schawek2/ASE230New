; Taken from module3/code/1_Docker/4.Containerizing Laravel with Docker/docker/php/php.ini

; Laravel optimized PHP configuration

; Memory and execution
memory_limit = 512M
max_execution_time = 300
max_input_time = 300

; File uploads
upload_max_filesize = 50M
post_max_size = 50M
max_file_uploads = 20

; Error reporting (development)
display_errors = On
display_startup_errors = On
error_reporting = E_ALL
log_errors = On
error_log = /var/log/php_errors.log

; Security settings
expose_php = Off
allow_url_fopen = On
allow_url_include = Off

; Session settings for Laravel
session.cookie_httponly = 1
session.use_strict_mode = 1
session.cookie_secure = 0
session.gc_maxlifetime = 7200

; Date settings
date.timezone = UTC

; OPcache settings for Laravel optimization
opcache.enable = 1
opcache.memory_consumption = 256
opcache.interned_strings_buffer = 16
opcache.max_accelerated_files = 10000
opcache.revalidate_freq = 2
opcache.save_comments = 1
opcache.enable_cli = 0
opcache.validate_timestamps = 1

; Realpath cache optimization for Laravel
realpath_cache_size = 4M
realpath_cache_ttl = 120

; Laravel specific optimizations
max_input_vars = 3000
variables_order = "GPCS"
request_order = "GP"

; FastCGI settings
fastcgi.logging = 1

; Development settings (override in production)
; display_errors = Off
; log_errors = On
; error_reporting = E_ALL & ~E_DEPRECATED & ~E_STRICT
